<section>
    Go Back to <a href="/user/homepage">Home</a>

    <h2>Change Password</h2>
    <% if (message) { %>
        <p>
            <%= message %>
        </p>
        <% } %>
            <form action="/user/change-password" method="post">
                <input type="password" name="oldPassword" placeholder="Old Password" required><br>
                <input type="password" name="newPassword" placeholder="New Password" required><br><br>
                <div class="g-recaptcha" data-sitekey="6LcmRHQpAAAAAKox1CmxwgV0Cbh03cU1MTc4si2g"></div>
                <button type="submit">Change Password</button>
                <button onclick="signOut()">Log Out</button>
            </form>

            <p>Don't have an account? <a href="/user/signup">Sign up</a></p>
            <p>Already have an account? <a href="/user/signin">Sign in</a></p>

</section>


<script>
    // Function to handle sign-out
    function signOut() {
        // Perform sign-out logic (e.g., redirect to sign-out route)
        window.location.href = "/user/signout";
    }

</script>

<!-- The line <div class="g-recaptcha" data-sitekey="6LcmRHQpAAAAAKox1CmxwgV0Cbh03cU1MTc4si2g"></div> 
integrates Google reCAPTCHA to protect the form against automated bot submissions.
data-sitekey: This is a public key used by the Google reCAPTCHA service to identify your site. -->


<!-- window.location.href = "/user/signout";
window.location.href is a property of the window object that represents the URL of the current web page.
Assigning a new value to window.location.href changes the URL of the page and effectively redirects the browser to the new URL.
In this case, the line window.location.href = "/user/signout"; means that when the signOut() function is called,
 the browser will be redirected to the /user/signout route. -->